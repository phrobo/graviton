set(GRAVITON_VERSION_MAJOR @GRAVITON_VERSION_MAJOR@)
set(GRAVITON_VERSION_MINOR @GRAVITON_VERSION_MINOR@)
set(GRAVITON_VERSION_PATCH @GRAVITON_VERSION_PATCH@)
set(GRAVITON_VERSION_PLATFORM @GRAVITON_VERSION_PLATFORM@)
set(GRAVITON_PLUGIN_PATH @GRAVITON_PLUGIN_PATH@)

find_package(PkgConfig)

pkg_check_modules(GRAVITON QUIET graviton-${GRAVITON_VERSION_PLATFORM})

get_filename_component(GRAVITON_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(${GRAVITON_CMAKE_DIR}/GravitonInternal.cmake)
